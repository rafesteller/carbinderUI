import { EmialDirective } from './emial.directive';

describe('EmialDirective', () => {
  it('should create an instance', () => {
    const directive = new EmialDirective();
    expect(directive).toBeTruthy();
  });
});
